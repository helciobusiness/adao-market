import { GraduationCap } from "lucide-react";

export const MethodBadge = () => (
    <div className="flex items-center gap-2 bg-blue-100 text-blue-900 px-4 py-1.5 rounded-full text-sm font-semibold border border-blue-200 w-fit shadow-sm">
        <GraduationCap size={16} />
        <span>Modo Académico – Metodologia da Disciplina</span>
    </div>
);
